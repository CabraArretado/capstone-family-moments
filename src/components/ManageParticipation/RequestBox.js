import React from "react"

//moods

const RequestBox = (props) => {
    return <div>
            <h4>Name: {props.user.firstname + " "+ props.user.lastname}</h4>
        </div>
}

export default RequestBox